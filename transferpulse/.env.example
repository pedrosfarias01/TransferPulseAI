# Copy to .env and paste your key.
OPENAI_API_KEY=sk-...

# Optional: override the Slack incoming-webhook alerts are posted to.
# Falls back to the built-in demo webhook in config.py if unset.
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...
